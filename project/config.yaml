dpi: 350
pdf2image_threads: 2

# ─── Torch & Parallelism ─────────────────────────────────
# number of intra‐op threads for PyTorch
torch_threads: 8
# number of inter‐op threads for PyTorch
torch_interop_threads: 4
# number of worker processes to run documents in parallel
num_workers: 4
# whether to convert models + tensors to half precision
half_precision: true

input_dir: data
image_dir: data/images
ocr_dir: data/ocr
blocks_dir: data/blocks
cleaned_dir: data/cleaned
